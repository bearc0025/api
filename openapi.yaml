openapi: 3.0.1
info:
  title: My SEB API
  version: '1.0'
  description: an api that fetches...
  termsOfService: https://www.example.com/tos
  contact:
    email: support@example.com
    name: Support
    url: https://www.example.com/contact
  license:
    name: License
    url: https://www.example.com/license
servers:
  - url: https://my-json-server.typicode.com/bearc0025/api
    variables: {}
    description: my fake typicode on github
paths:
  /users:
    summary: user level operations
    description: fetch users, create new user
    get:
      summary: fetch users from server
      description: returns user objects from server
      operationId: fetchUsers
      responses:
        default:
          description: Default error sample response
      tags:
        - users
